{
  "name": "brettreinhard.com",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://brettreinhard.com/",
  "dependencies": {
    "autocomplete-react-component": "0.0.16",
    "booklet-react-component": "0.0.10",
    "bootstrap": "^3.3.7",
    "font-awesome": "^4.7.0",
    "react": "^15.6.1",
    "react-addons-css-transition-group": "^15.6.0",
    "react-bootstrap": "^0.31.2",
    "react-dom": "^15.6.1",
    "react-router-bootstrap": "^0.24.2",
    "react-router-dom": "^4.2.2",
    "react-scripts": "5.0.1",
    "react-waypoint": "^7.1.0",
    "typed-react-component": "0.0.4"
  },
  "scripts": {
    "deploy": "sudo chown -R brett *&&sudo git add *&&sudo git commit -m \"Updated Site\"&&sudo git push&&sudo git checkout gh-pages&&sudo git pull&&sudo chown -R brett ../bareinhard.github.io&&sudo rm -R ../bareinhard.github.io/* &&sudo cp -R * ../bareinhard.github.io/ &&sudo cd ../bareinhard.github.io &&sudo git add * &&sudo git commit -m \"Update WebPage\"&&git push&&sudo cd ../brett.reinhard.com&&sudo git checkout master",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0"
  }
}
